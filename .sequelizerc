import { resolve } from 'path';

module.exports = {
    config: resolve(__dirname, 'src', 'config', 'db.ts'),
    'migrations-path': resolve(__dirname, 'src', 'shared', 'database', 'migrations'),
    'seeders-path': resolve(__dirname, 'src', 'shared', 'database', 'seeds'),

};
